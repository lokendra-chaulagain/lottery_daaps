import React from "react";
import { useAppContext } from "../../context/context";

const PlayerTable = () => {
  const { lotteryPlayers } = useAppContext();
  //map the players thats it

  return <div>PlayerTable</div>;
};

export default PlayerTable;
